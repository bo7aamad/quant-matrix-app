name: Build Standalone Android APK
on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
    - name: Checkout Repository Source
      uses: actions/checkout@v4

    - name: Setup System Dependencies
      run: |
        sudo apt-get update
        # Fixed: Exchanged the invalid envsubst reference for python3-dev and zlib1g-dev compilation engines
        sudo apt-get install -y build-essential ccache lbzip2 libffi-dev libssl-dev autoconf automake libtool pkg-config cmake git openjdk-17-jdk unzip zip zlib1g-dev python3-dev

    - name: Set up Python Environment
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install Buildozer Framework
      run: |
        pip install --upgrade pip
        pip install "cython<3.0.0" buildozer

    - name: Build Android Native Binaries via Cloud Action
      run: |
        export APP_ANDROID_ARCHS="arm64-v8a"
        yes | buildozer android debug
      continue-on-error: false

    - name: Upload Standalone App Installer Artifact
      uses: actions/upload-artifact@v4
      with:
        name: Standalone-Quant-Package
        path: bin/*.apk
